<% items ||= Item.alphabetical %>
<% items = items.active + items.inactive %>
<div class="row">
  <% for item in items %>
    <div class="col s3 m2">
      <%= render partial: 'partials/item_preview', locals: { item: item } %>
    </div>
  <% end %>
</div>
