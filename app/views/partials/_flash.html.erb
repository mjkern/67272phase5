<% flash.each do |name, msg| %>
    <script type="text/javascript">
      M.toast({html: '<%= msg %>', display_length: 6000});
    </script>
    <!--
  <div data-alert data-options="animation_speed:500, animation:'fadeOut';" class="alert-box <%#= flash[:notice] ? "success" : "warning" %>">
    <%#= msg %>
  </div>-->
<% end %>
